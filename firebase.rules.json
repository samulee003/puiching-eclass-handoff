{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        "todos": {
          "$itemId": {
            ".write": "auth != null",
            ".validate": "newData.hasChildren(['key', 'done', 'updatedAt', 'deviceId']) && newData.child('key').isString() && newData.child('done').isBoolean() && newData.child('updatedAt').isNumber() && newData.child('deviceId').isString() && newData.child('deviceId').val() == auth.uid"
          }
        },
        "project": {
          "$itemId": {
            ".write": "auth != null",
            ".validate": "newData.hasChildren(['key', 'status', 'updatedAt', 'deviceId']) && newData.child('key').isString() && (newData.child('status').val() == 'todo' || newData.child('status').val() == 'doing' || newData.child('status').val() == 'done') && newData.child('updatedAt').isNumber() && newData.child('deviceId').isString() && newData.child('deviceId').val() == auth.uid"
          }
        }
      }
    }
  }
}
